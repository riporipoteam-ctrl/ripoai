# iOS sideloader tools (libimobiledevice, zsign) are downloaded here by CI
# (.github/workflows/windows-app.yml) and bundled into the installer at
# resources/tools. You can also drop the .exe binaries here manually.
